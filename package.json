{
  "name": "story-bot-core",
  "version": "1.0.0",
  "description": "story bot core part",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist\\main.js",
    "build": "babel src --out-dir dist",
    "watch": "babel src --watch --out-dir dist"
  },
  "author": "storycraft",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^11.3.0",
    "facebook-chat-api": "github:Schmavery/facebook-chat-api",
    "linebot": "^1.4.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-polyfill": "^6.26.0"
  }
}
